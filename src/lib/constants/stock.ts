/**
 * Products with stock_qty at or below this threshold are considered "low stock".
 * Used by both the dashboard stat card and the Products page filter to ensure consistency.
 */
export const LOW_STOCK_THRESHOLD = 10;
